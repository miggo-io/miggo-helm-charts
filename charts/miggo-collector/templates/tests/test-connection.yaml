apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "miggo-collector.fullname" . }}-test-connection"
  labels:
    {{- include "miggo-collector.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "miggo-collector.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
