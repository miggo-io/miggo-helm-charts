{{- if .Values.networkPolicy.enabled }}
{{- if .Values.miggoRuntime.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "miggo.fullname" . }}-runtime
  namespace: {{ include "miggo.namespace" . }}
  labels:
    {{- include "miggoRuntime.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "miggoRuntime.selectorLabels" . | nindent 6 }}
  policyTypes:
    - Ingress
  ingress:
    - ports:
        - protocol: TCP
          port: {{ .Values.healthcheck.port }}
    {{- if .Values.pprof.port }}
    - from:
        - podSelector:
            matchLabels:
              {{- include "miggo-collector.selectorLabels" . | nindent 14 }}
      ports:
        - protocol: TCP
          port: {{ .Values.pprof.port }}
    {{- end }}
    {{- if .Values.pprof.profilerPort }}
    - from:
        - podSelector:
            matchLabels:
              {{- include "miggo-collector.selectorLabels" . | nindent 14 }}
      ports:
        - protocol: TCP
          port: {{ .Values.pprof.profilerPort }}
    {{- end }}
{{- end }}
{{- end }}
