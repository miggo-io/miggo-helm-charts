{{- if .Values.networkPolicy.enabled }}
{{- if .Values.miggoCollector.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "miggo.fullname" . }}-collector
  namespace: {{ include "miggo.namespace" . }}
  labels:
    {{- include "miggo-collector.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "miggo-collector.selectorLabels" . | nindent 6 }}
  policyTypes:
    - Ingress
  ingress:
    - ports:
        - protocol: TCP
          port: {{ .Values.healthcheck.port }}
    - from:
        - podSelector:
            matchLabels:
              {{- include "miggo-watch.selectorLabels" . | nindent 14 }}
        - podSelector:
            matchLabels:
              {{- include "miggo-scanner.selectorLabels" . | nindent 14 }}
        - podSelector:
            matchLabels:
              {{- include "miggoRuntime.selectorLabels" . | nindent 14 }}
      ports:
        - protocol: TCP
          port: 4318
    - from:
        - podSelector:
            matchLabels:
              {{- include "miggoRuntime.selectorLabels" . | nindent 14 }}
      ports:
        - protocol: TCP
          port: 4317
{{- end }}
{{- end }}
