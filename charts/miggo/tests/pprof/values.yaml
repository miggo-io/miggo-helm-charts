miggo:
  clusterName: "pprof-test"

config:
  accessKey: "test-key"

miggoWatch:
  enabled: true

miggoScanner:
  enabled: true

miggoRuntime:
  enabled: true
  profiler:
    enabled: false

miggoCollector:
  enabled: true

pprof:
  port: 0
  profilerPort: 0
