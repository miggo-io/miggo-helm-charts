name: CI

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main

permissions:
  contents: write
  pull-requests: write

jobs:
  verify-and-bump:
    needs: generate-helm-docs
    uses: ./.github/workflows/bump-version.yaml
    
  helm-test:
    uses: ./.github/workflows/run-unittests.yaml

  generate-helm-docs:
    needs: helm-test
    uses: ./.github/workflows/run-helm-docs.yaml
